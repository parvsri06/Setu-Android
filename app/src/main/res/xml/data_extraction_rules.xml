<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing leaves the device through backup or transfer. The identity seed is
     sealed to a keystore key that does not survive a device transfer anyway, and
     the store holds other people's sealed messages, which have no business in a
     cloud backup. See docs/04-security-model.md. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
